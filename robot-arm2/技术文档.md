# 六自由度机械臂 · 系统技术文档

一份从硬件到云端的完整技术说明，解释这个系统**由什么组成**、**怎么运作**、以及**每个技术选择背后的原因**。

---

# 一、系统总览

## 它是什么

一台 3D 打印的六自由度机械臂，可以通过网页远程控制，也能用手势识别驱动。

机械结构采用 **SO-ARM101** 开源设计（Hugging Face LeRobot 项目的官方硬件）。

**六个自由度**（从下到上，与舵机编号一致）：

| 编号 | 关节 | 作用 |
|---|---|---|
| 0 | 底座旋转 | 水平旋转 |
| 1 | 肩部 | 抬升/下降 |
| 2 | 肘部 | 俯仰 |
| 3 | 腕俯仰 | 调整末端角度 |
| 4 | 腕旋转 | 末端自转 |
| 5 | 夹爪 | 抓取/松开 |

## 四层架构

```
┌─────────────────────────────────────────────────────┐
│  应用层    Vue 3 网页遥控  │  Python 手势识别            │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP
┌──────────────────────▼──────────────────────────────┐
│  云服务层   Node.js + Express + WebSocket 中转         │
│            MySQL 数据持久化  │  OpenAPI 接口文档         │
└──────────────────────┬──────────────────────────────┘
                       │ WebSocket 长连接
┌──────────────────────▼──────────────────────────────┐
│  嵌入式层   ESP32-S3 固件（C++ / Arduino）             │
└──────────────────────┬──────────────────────────────┘
                       │ I2C（当前）/ TTL 串口总线（升级后）
┌──────────────────────▼──────────────────────────────┐
│  硬件层     PCA9685 驱动板  │  舵机 × 5  │  3D 打印结构  │
│            UBEC 降压  │  锂电池  │  分电盒               │
└─────────────────────────────────────────────────────┘
```

---

# 二、一次完整的数据流

**以"在网页上把底座转到 45°"为例，追踪整条链路。**

### ① 用户拖动滑块

浏览器里的 Vue 组件 `ServoCard.vue` 收到 `input` 事件，通过 `defineModel()` 把新值同步给父组件。

### ② 前端节流

`useArmControl.js` 里的 `setAngle()` 做 **60ms 节流**——拖动时每秒触发几十次事件，每次都发请求会把 ESP32 打爆。节流后只发最后一次。

```js
fetch('/set?servo=0&angle=45')
```

### ③ HTTP 请求到达服务器

Express 路由 `app.get('/set')` 命中：

```js
const cmd = servo + ':' + angle;   // "0:45"
if (esp32) esp32.send(cmd);        // 通过 WebSocket 发出
else res.status(503)               // 设备不在线
```

**注意 503**：网页和手势模块都用这个状态码判断"机械臂是否在线"。

### ④ WebSocket 转发到 ESP32

ESP32 在局域网/内网，没有公网 IP。它在启动时就**主动连出来**，和服务器建立一条长连接并保持。服务器拿到浏览器命令后，顺着这条已有连接往回发。

### ⑤ 固件解析命令

```cpp
void onMessage(WStype_t type, uint8_t* payload, size_t length) {
  String msg = String((char*)payload);   // "0:45"
  int idx = msg.substring(0, colon).toInt();   // 0
  int tgt = msg.substring(colon + 1).toInt();  // 45
  target[idx] = tgt;                      // 只记目标值，不立即执行
}
```

### ⑥ 主循环分步推进

```cpp
void loop() {
  ws.loop();        // 保持通信，必须高频调用
  stepMotion();     // 每 15ms 推进一步
}
```

`stepMotion()` 遍历所有舵机，凡是当前值 ≠ 目标值的就 ±1 度。**五个舵机因此能并行运动**，而且 `loop()` 始终不阻塞。

### ⑦ 写入 PCA9685

```cpp
pwm.setPWM(CH[i], 0, angleToPWM(a));
```

这行代码**不是直接输出 PWM**，而是通过 **I2C 总线**把"目标脉宽"写进 PCA9685 的寄存器。

### ⑧ 舵机转动

PCA9685 收到寄存器值后，**用自己的晶振硬件产生 50Hz 的 PWM 波形**，持续输出，直到下次被改写。舵机内部的电位器检测当前角度，与脉宽对应的目标角度比较，驱动电机转到位。

### ⑨ 到位回报

所有舵机都到达目标后，固件把当前状态回传：

```
0:45,1:90,2:90,3:90,4:90
```

服务器日志打印出来，网页端可据此显示"已到位"。

---

# 三、硬件层

## 舵机

**模拟 PWM 舵机**（MG996R 类）：

| 参数 | 值 |
|---|---|
| 工作电压 | 4.8 – 7.2 V |
| 控制方式 | PWM，50Hz |
| 脉宽范围 | 0.5 – 2.5 ms 对应 0 – 180° |
| 堵转电流 | 约 1.5 A |

**工作原理**：舵机内部有电位器检测当前角度，控制电路把"脉宽长度"翻译成目标角度，两者比较后驱动电机转动，直到一致。所以它**只认脉宽**——脉宽被干扰改变几个微秒，舵机就会当成一次角度变化。

> ⚠️ 这正是本项目"抖动问题"的根源，详见第六节。

## 驱动板 PCA9685

**为什么需要它**：ESP32 的硬件 PWM（LEDC）通道数量有限，且用它直接驱动 5 路舵机会让信号线从 ESP32 一路拉到机械臂——线长、贴近天线。

PCA9685 是 **I2C 转 16 路 PWM** 的驱动芯片：

```
ESP32 ──I2C（2 根线）──> PCA9685 ──PWM（16 路）──> 舵机
```

**好处**：
- ESP32 只需 2 根线（SDA/SCL）就能控制 16 路
- PWM 由 PCA9685 **自己产生**，ESP32 不参与，不受其 WiFi 活动影响
- 可以把 PCA9685 放在**靠近舵机**的位置，缩短 PWM 信号线

## 供电

```
12.6V 锂电池 ──> UBEC 降压 5V ──> 2进12出分电盒 ──> 5 路舵机
                                                    └──> 一根地线回 ESP32 共地
```

**为什么要独立供电**：5 个舵机同时动作时峰值电流可达数安培。如果从 ESP32 取电，电压会被拉垮导致单片机复位。

**必须共地**：ESP32 和舵机电源的 GND 必须接在一起，否则 PWM 信号没有共同的参考电平。

---

# 四、嵌入式层

## ESP32-S3

- 双核 Xtensa LX7，240MHz
- 内置 **2.4GHz WiFi**（本项目用它的 STA 模式连路由器）
- 硬件外设：I2C、UART、LEDC(PWM) 等

## 固件的两种模式

| 模式 | 代表固件 | 特点 |
|---|---|---|
| **softAP 自建热点** | `servo-test.ino` | ESP32 自己开热点，浏览器直连它的 IP。简单，但**只能局域网** |
| **STA 连路由器 + WebSocket** | `arm-cloud.ino` | ESP32 主动连服务器，可**公网访问** |

## 非阻塞运动控制

**反面教材**（早期写法）：

```cpp
while (angles[idx] != tgt) {      // ❌ 阻塞
  angles[idx] += step;
  writeServo(idx, angles[idx]);
  delay(15);
}
```

问题有二：
1. `delay` 期间 `loop()` 卡死，**WebSocket 收不到新消息、心跳发不出去**，服务器会判定设备掉线
2. 多轴只能一个一个动

**正确写法**：

```cpp
void loop() {
  ws.loop();       // 通信优先
  stepMotion();    // 所有轴并行推进
}
```

## 通信健壮性

```cpp
ws.setReconnectInterval(5000);        // 断了自动重连
ws.enableHeartbeat(15000, 3000, 2);   // 心跳保活
```

**心跳为什么必要**：家用路由器和运营商的 NAT 会掐断长时间空闲的 TCP 连接。没有心跳的话，连接看着还在，实际已经死了，命令发不进去。

---

# 五、云服务层

## 为什么需要服务器中转

**核心矛盾**：ESP32 在局域网里，**没有公网 IP**。

三种解法对比：

| 方案 | 问题 |
|---|---|
| 端口映射 | 需要路由器管理权限，且暴露家庭内网 |
| 内网穿透（frp/ngrok） | 要额外跑一个服务，免费版不稳定 |
| **设备主动连出（本项目）** | ESP32 主动连服务器并保持长连接，服务器顺着这条连接往回发命令 |

第三种是**物联网设备的标准做法**——不需要公网 IP，不用改路由器。

## 技术栈

**Express**：提供 REST API 和静态站点托管

**ws 库**：WebSocket 服务端，与 Express 共用同一个 HTTP server

**设备鉴权**：ESP32 连接时必须在地址里带 token

```js
const token = url.searchParams.get('token');
if (token !== config.ESP32_TOKEN) {
  ws.close();   // 不对就断开
}
```

**为什么需要**：否则任何人只要知道服务器地址，就能伪造设备连接、劫持控制权。

## MySQL 数据持久化

留言板从 JSON 文件迁移到 MySQL：

| | JSON 文件 | MySQL |
|---|---|---|
| 并发 | 全量读写，有竞态 | 行级锁，并发安全 |
| 查询 | 全部读进内存 | 索引加速 |
| 关联 | 手工维护 | 外键约束 |

**两个具体优化**（详见 `server/DATABASE.md`）：

- **索引优化**：给 `created_at` 加索引后，列表查询扫描行数从 **49914 → 20**，耗时 **11.76ms → 0.30ms**
- **消除 N+1 查询**：列表接口从"1 条留言查 1 次回复"（1+N 次查询）改为用 `IN` 批量取回再内存归组（**固定 2 次**）

## 接口文档

**OpenAPI 3.0 + Swagger UI**，注解以 JSDoc 形式写在路由上方，文档与代码同步维护。

访问 `/api-docs` 可在线调试，`/api-docs.json` 可导入 Postman。

---

# 六、关键技术问题：抖动

这是本项目**最有价值的一段工程经历**。

## 现象

5 个舵机静止锁定时，全部有小幅抽搐，**不同步**。空载也抖。

## 排查过程（逐步排除）

| 怀疑对象 | 验证方法 | 结论 |
|---|---|---|
| PCA9685 板坏了 | 拆掉改直驱，仍抖 | ❌ 排除 |
| 个别舵机坏 | 全部都会抖 | ❌ 排除 |
| 供电不足 | 5V 20A 余量充足，空载也抖 | ❌ 排除 |
| 机械虚位 | 空载也抖 | ❌ 排除 |
| **WiFi 射频** | **关掉 WiFi 烧录 → 完全不抖** | ✅ **确诊** |

## 根因

**ESP32 的 2.4GHz WiFi 射频耦合进了那 5 根未屏蔽的信号线。**

模拟舵机靠"量脉宽长度"决定角度。射频毛刺叠上去，脉宽被撑长或削短几微秒，舵机就把它当成一次角度变化 → 抽搐。不同舵机耦合强度不同 → 看起来"异步"。

## 为什么直驱反而更糟

| | PCA9685 方案 | 直驱方案 |
|---|---|---|
| PWM 从哪来 | PCA 芯片自己产生 | ESP32 GPIO |
| 信号线起点 | 从 PCA 出发（**远离射频源**） | **从 ESP32 出发（就是射频源）** |
| 信号线长度 | 短 | 长 |

直驱把"信号源"搬到了射频源旁边，还顺手加长了天线。

## 两种解决思路

**思路 A：信号完整性层面压制**（治标）

- 每根信号线串 100Ω 电阻，磨圆陡边沿
- 每个舵机信号端对地并 1–4.7nF 电容，旁路射频
- 星形接地，信号线远离天线

成功率约 70–85%，需要反复调试。

**思路 B：换架构**（治本）

改用**串行总线舵机**（如 Feetech STS3215）：

```
传统舵机：  ──PWM 脉宽（模拟量）──>  舵机
总线舵机：  ──数字协议帧 + 校验和──>  舵机
```

总线舵机的指令是有帧格式的：

```
0xFF 0xFF ID LENGTH INSTRUCTION 参数... 校验和
```

噪声混进来，要么被数字电平阈值滤掉，要么**造成校验失败被舵机直接丢弃**——永远不会翻译成角度。

**这是协议层面的免疫，不是靠滤波压制。**

**附加收益**：总线舵机是闭环的，能回传当前位置和**电流**。电流 ≈ 力矩，于是可以做**力反馈**——夹爪抓握力度控制、碰撞检测、堵转保护。这是模拟舵机做不到的（它只有一根信号线进，什么都不回）。

---

# 七、应用层

## Vue 3 网页遥控

**技术栈**：Vue 3.5 + Vite 8 + 单文件组件

**目录结构**：

```
web/src/
├── App.vue                      主页面
├── components/ServoCard.vue     舵机卡片（可复用组件）
└── composables/useArmControl.js 控制逻辑
```

**设计要点**：

- **逻辑与视图分离** —— 控制逻辑全在 composable 里，组件只管渲染。将来换成 WebSocket 或接入手势输入，组件一行都不用改
- **`defineModel()` 双向绑定** —— 子组件不需要知道父组件怎么存数据
- **节流** —— 拖动只发最后一次，按钮点击不节流（保证即时响应）

## Python 手势识别

**技术栈**：MediaPipe + OpenCV

**流程**：摄像头 → 手部 21 个关键点 → 手指伸展判定 → 手势分类 → HTTP 命令

**核心算法**：判断手指是否伸直，常见做法是"指尖 y 坐标比指节小"，但**这要求手掌必须竖直朝上**，手一歪就判错。

本项目改用**距离比较**：

```
伸直 → 指尖伸得更远 → 指尖到掌心的距离 > 指节到掌心的距离
弯曲 → 指尖收回掌心 → 距离反而更小
```

这个方法与**手掌朝向、旋转、离镜头远近都无关**，稳得多。

**两种模式**：
- 指令模式：固定手势触发固定动作（张开/握拳/数字…）
- 跟随模式：手掌位置实时映射到底座和大臂角度

---

# 八、技术栈总览

| 层面 | 技术 |
|---|---|
| **嵌入式** | C++ / Arduino、ESP32-S3、I2C、PWM、TTL 串口协议、WebSocket 客户端 |
| **后端** | Node.js、Express、ws（WebSocket）、MySQL、连接池、参数化查询 |
| **前端** | Vue 3（Composition API / SFC）、Vite、HTML/CSS/JS、PWA |
| **AI** | Python、MediaPipe、OpenCV |
| **数据库** | MySQL、索引优化、EXPLAIN 分析、N+1 优化、外键约束 |
| **接口文档** | OpenAPI 3.0、Swagger UI |
| **工程工具** | Git、uv、npm、Arduino CLI、MySQL |
| **部署** | Linux、Nginx、SSL |

---

# 九、项目演进时间线

```
1. 基础遥控        ESP32 softAP + PCA9685 + 网页滑块
2. 云化改造        ESP32 改 STA 模式 + WebSocket 客户端 → 公网可控
3. 发现抖动        5 个舵机静止时小幅抽搐
4. 排除法定位      逐个排除硬件嫌疑，最终确诊为 WiFi 射频耦合
5. 整改变压        信号侧加串阻/电容/星形接地（压制，治标）
6. 架构升级        换总线舵机（协议层免疫，治本）+ 力反馈
7. 功能扩展        手势识别 + MySQL + Vue 3 重写 + Swagger 文档
```

---

# 十、可延伸方向

| 方向 | 说明 |
|---|---|
| **力反馈** | 读取总线舵机的电流，实现夹爪力度控制、碰撞检测 |
| **逆运动学** | 给定末端坐标，反解各关节角度，实现"告诉我位置而不是角度" |
| **轨迹规划** | 直线插补、圆弧插补，让末端走平滑路径 |
| **视觉抓取** | 摄像头识别物体位置 → 逆运动学解算 → 自动抓取 |
| **多机协同** | 服务器已支持设备鉴权，扩展成多设备管理即可 |

---

## 相关文档

| 文档 | 内容 |
|---|---|
| [`server/README.md`](server/README.md) | 服务器架构、部署、接口速查 |
| [`server/DATABASE.md`](server/DATABASE.md) | 数据库设计与 SQL 优化实证 |
| [`server/web/README.md`](server/web/README.md) | Vue 前端设计说明 |
| [`gesture-control/README.md`](gesture-control/README.md) | 手势识别模块 |
| [`arm-cloud-bus/`](arm-cloud-bus/) | 云连接 + 总线舵机固件 |
| [`arm-bus-servo/README.md`](arm-bus-servo/README.md) | 总线舵机升级指南 |
| [抖动诊断结论.md](../robot-arm%201/wifi-off-hold-test/抖动诊断结论.md) | 抖动问题的诊断报告（第一代归档） |
