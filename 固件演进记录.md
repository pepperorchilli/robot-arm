# 固件演进记录

记录每一版固件的用途、被取代的原因，以及项目在每个阶段解决的实际问题。

> 这份记录的价值在于：**它展示了技术方案是怎么一步步演化出来的**，
> 而不是一开始就设计好。每个版本都对应一个真实遇到的问题。

---

## 一、版本总览

固件分属两个阶段的目录：

- **`robot-arm 1/`** —— 第一代（2026-08-16 冻结快照）
- **`robot-arm2/`** —— 第二代（当前版本）

| 固件 | 位置 | 时期 | 定位 | 状态 |
|---|---|---|---|---|
| `servo-test/` | robot-arm 1 | 最早 | 基础遥控：softAP + PCA9685 + 网页滑块 | 🔵 已归档 |
| `servo-direct-test/` | robot-arm 1 | 08-15 | 故障排查：单舵机直驱，判断抖动来源 | 🔵 已归档 |
| `arm-5servo/` | robot-arm 1 | 08-15 | 备用方案：绕开 PCA9685 直驱 5 路 | 🔵 已归档 |
| `wifi-off-hold-test/` | **robot-arm2** | 09-02 | **决定性实验**：关 WiFi 验证抖动根因 | 🔵 诊断工具 |
| `arm-cloud/` | **robot-arm2** | 08-13<br>09-18 重写 | **云连接版**：STA + WebSocket + PCA9685 | 🟢 当前可用 |
| `arm-bus-servo/` | **robot-arm2** | 09-18 | 总线舵机版：softAP + STS3215 | 🟡 待上机 |
| `arm-cloud-bus/` | **robot-arm2** | 09-18 | **合并版**：云连接 + 总线舵机 | 🟡 待上机 |
| `servo-direct/` | — | — | 已删除的历史版本 | ⚫ 见 git 历史 |
| `servo-test/servo-single.ino` | — | 最早 | 已删除：单舵机测试 | ⚫ 见 git 历史 |

> 📌 **为什么第一代的固件不再更新**：它们已被 `arm-cloud*` 系列取代
> （都实现了云连接，且 `arm-cloud-bus` 还解决了抖动问题）。
> 保留它们是为了记录技术演进 —— 面试时"为什么从 A 换到 B"比"我会用 B"更有说服力。

---

## 二、时间线

```
2026-08-11  ┌─ WiFi STA 模式连接手机热点
            │  从"ESP32 自建热点"迈出第一步，让它能连外部网络
            │
2026-08-13  ├─ 云服务器 WebSocket 中转 + 网页远程控制   ← 里程碑
            │  ESP32 主动连出，实现公网可控
            │  ⚠️ 此时 WiFi 密码被硬编码在固件里提交到了公开仓库
            │     （已于 09-18 从全部历史中清除）
            │
2026-08-15  ├─ 舵机信号/电源分离接线，新增直驱诊断代码
            │  发现抖动问题，开始排查
            │
2026-08-15  ├─ 删除直驱诊断代码 servo-direct
            │
2026-08-16  ├─ 产品官网（留言板/下载/PWA）+ WebSocket 设备鉴权
            │  加入 token 校验，防止伪造设备连接
            │
2026-09-02  ├─ 抖动诊断：关 WiFi 锁死测试
            │  ★ 决定性实验 —— 关掉 WiFi 后完全不抖，确诊为射频耦合
            │
2026-09-18  ├─ 手势识别控制 + 留言板接入 MySQL
            ├─ 总线舵机固件（STS3215）与升级文档
            ├─ 遥控页改用 Vue 3 + Vite 重写
            ├─ 接入 Swagger 接口文档
            ├─ 云连接版固件（从历史恢复并升级）
            │
2026-09-18  └─ 系统技术文档 + 根 README + 本演进记录
```

---

## 三、关键转折点

### 转折 1：从局域网到公网（08-13）

**问题**：ESP32 在局域网里，没有公网 IP，外面访问不进来。

**走过的路**：先试了 softAP 自建热点（`servo-test/`），只能局域网直连。

**最终方案**：让 ESP32 **主动连出去**，和服务器保持 WebSocket 长连接，服务器顺着这条连接把命令转发进来。

```
❌ 端口映射      —— 要路由器权限，且暴露内网
❌ 内网穿透      —— 要跑额外服务，免费版不稳
✅ 设备主动连出  —— 物联网设备的标准做法
```

### 转折 2：抖动问题（08-15 发现 → 09-02 确诊）

**现象**：5 个舵机静止锁定时小幅抽搐，**不同步**，空载也抖。

**排查**（逐步排除，最终锁定）：

| 怀疑对象 | 验证方法 | 结论 |
|---|---|---|
| PCA9685 | 拆掉改直驱，仍抖 | ❌ 排除 |
| 舵机个体 | 全部都会抖 | ❌ 排除 |
| 供电不足 | 5V 20A 余量充足，空载也抖 | ❌ 排除 |
| 机械虚位 | 空载也抖 | ❌ 排除 |
| **WiFi 射频** | **关掉 WiFi 烧录 → 完全不抖** | ✅ **确诊** |

**根因**：模拟舵机靠"量脉宽长度"决定角度，ESP32 的 2.4GHz 射频耦合进未屏蔽的信号线，让脉宽被撑长/削短几微秒，舵机当成一次角度变化。

**意外发现**：绕开 PCA9685 改直驱**反而更糟**——因为直驱把信号源搬到了射频源（ESP32）旁边，还把信号线加长了。

**解决思路**：
- 治标：信号线串 100Ω + 舵机端对地并小电容 + 星形接地（约 70–85% 成功率）
- **治本：换串行总线舵机** —— 数字协议带校验和，噪声要么被电平阈值滤掉、要么校验失败被丢弃，属于**协议层免疫**

### 转折 3：安全整改（09-18）

**问题**：排查时发现，早期固件把 **WiFi 密码明文硬编码**并提交到了**公开仓库**，在历史里存在了几个月。

**处理**：
1. 用 `git filter-repo` 从**全部提交历史**中清除（不只是删文件）
2. 凭据抽离到 `secrets.h`，加入 `.gitignore`
3. 提供 `secrets.h.example` 作为模板

**教训**：清除历史 ≠ 撤销泄露。凭据一旦公开过，**唯一可靠的止损是更换凭据本身**。

---

## 四、固件选型指南

现在手上有多个固件，按需求选：

| 你的情况 | 用哪个 |
|---|---|
| 只想台架测试舵机，不接服务器 | `robot-arm 1/servo-test/`（softAP + 网页直连） |
| 要用云链路 + 现有 MG996R 舵机 | **`robot-arm2/arm-cloud/`** |
| 要用云链路 + 已换总线舵机 | **`robot-arm2/arm-cloud-bus/`** |
| 单独验证总线舵机协议 | `robot-arm2/arm-bus-servo/`（带串口调试命令） |
| 排查抖动 / 做对比实验 | `robot-arm2/wifi-off-hold-test/`、`robot-arm 1/servo-direct-test/` |

---

## 五、云端固件的两代对比

`arm-cloud/` 和 `arm-cloud-bus/` 上层完全一样，只有底层驱动不同：

| | `arm-cloud/` | `arm-cloud-bus/` |
|---|---|---|
| 舵机类型 | 模拟 PWM（MG996R） | 总线舵机（STS3215） |
| 驱动方式 | I2C → PCA9685 → PWM | TTL 串口 → 直接控制 |
| 运动控制 | **软件插补**：主循环每 15ms 逐度推进 | **硬件平滑**：下发"目标+时间"，舵机自己走 |
| 多轴同步 | 逐个写寄存器 | **SYNC_WRITE** 一条指令广播 |
| 抗射频干扰 | 需要硬件滤波 | 协议层免疫 |
| 力反馈 | ❌ 不支持 | ✅ 可读电流/位置 |
| 供电 | 5V | **7.4V** |

**注意**：总线版不需要软件插补，代码反而更简单——这是一个典型的"选对架构，代码自然简化"的例子。

---

## 六、如何找回已删除的历史版本

```bash
# 列出所有历史上出现过的固件文件
git log --all --pretty=format: --name-only --diff-filter=A | grep '\.ino' | sort -u

# 查看某个文件在历史上的所有版本
git log --all --oneline -- <路径>

# 取出某个版本的内容
git show <提交哈希>:<文件路径>
```

> 建议：**不要删除旧固件**。它们是项目演进的证据，
> 面试时"你为什么从 A 方案换到 B 方案"比"我会用 B"更有说服力。
